import React, { FC } from 'react';
import RegForm from '../../components/auth/AuthForm';

const Auth: FC = () => {
  return (
    <div className="Auth">
      <RegForm />
    </div>
  );
};

export default Auth;
